---
platform: linux
image_resource:
  type: registry-image
  source: { repository: node, tag: "12" }
inputs:
  - name: protect-sdk-js-dev
outputs:
  - name: protect-sdk-js-dev
run:
  path: /bin/sh
  args:
    - -c
    - |

      echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > ~/.npmrc

      cd protect-sdk-js-dev

      if ! yarn install
      then
        echo "ERROR: yarn install failed"
        exit 1
      fi

      if ! yarn build:prod
      then
        echo "ERROR: yarn build:prod has failed"
        exit 1
      fi
